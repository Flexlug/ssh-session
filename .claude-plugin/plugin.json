{
  "name": "ssh-session",
  "description": "Run commands on a remote host over a persistent SSH session instead of a bare `ssh host \"cmd\"` — one live connection per named session, so cwd, env vars, venvs and background jobs survive between tool calls, and interactive prompts can be answered.",
  "version": "1.2.0",
  "author": {
    "name": "Flexlug",
    "url": "https://github.com/Flexlug"
  },
  "homepage": "https://github.com/Flexlug/ssh-session",
  "repository": "https://github.com/Flexlug/ssh-session",
  "license": "MIT",
  "keywords": ["ssh", "remote", "pty", "automation", "devops"]
}
